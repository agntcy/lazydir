repos:
  - repo: local
    hooks:
      - id: lint
        name: lint
        description: Run linters via task.
        entry: task lint
        language: system
        pass_filenames: false
        types: [go]
        require_serial: true
